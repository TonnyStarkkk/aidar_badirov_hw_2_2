public abstract class SportCar implements Printable{
    double accelerationTime;
    int maxSpeed;

    public SportCar(double accelerationTime, int maxSpeed) {
        this.accelerationTime = accelerationTime;
        this.maxSpeed = maxSpeed;
    }
}
